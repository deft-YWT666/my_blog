# 排列好题

##  [46. 全排列 - 力扣（LeetCode）](https://leetcode.cn/problems/permutations/) 

## 题解

(1) 题意 : 返回 nums 数组的全排列

(2) 思路 : dfs回溯

## 代码(c++)

```cpp
class Solution {
public:
    vector<int> path ;
    vector<vector<int>> res ;
    void dfs(vector<int>& nums , vector<bool>& used )
    {
        if(path.size() == nums.size())
        {
            res.push_back(path) ;
            return ;
        }
        for(int i = 0 ; i < nums.size() ; i++)
        {
            if(used[i]) continue ;

            used[i] = true ;
            path.push_back(nums[i]) ;

            dfs(nums , used) ;

            used[i] = false ;
            path.pop_back() ;   
        }
    }
    vector<vector<int>> permute(vector<int>& nums) {
        vector<bool> used(nums.size() , false) ;
        dfs(nums , used) ;
        return res ;
    }
};

```





##  [P1036 [NOIP 2002 普及组\] 选数 - 洛谷](https://www.luogu.com.cn/problem/P1036) 

### 题解

(1) 题意 : 我们要在 n 个数字里面选 k 个数字 ,  有多少种加起来为素数的选法

(2) 思路 : 看到 n <= 20 , 果断采用暴力算法 , 把所有情况都枚举出来 , 然后判断是不是素数 . 第一种思路: 新定义一个数组 , 标记其中 k 个数字为 1 其余为 0 , 使用next_permutation( ) 把所有的 01 串排列出来 , 把标记为 1 的地方加起来. 

### 代码

```cpp
#include<bits/stdc++.h>
#define int long long


using namespace std ;

const int N = 22 ;
int n , k  , ans ;
int a[N] , pos[N] ;

bool is_prime(int x){
    if(x <= 1) return false ;
    for(int i = 2 ; i <= x / i ; i++){
        if(x % i == 0) return false ;
    }
    return true ;
}

void solve(){
    cin >> n >> k ;
    
    for(int i = 1 ; i <= n ; i++){
        cin >> a[i] ;
    }
    
    for(int i = 1; i <= k ; i++){
        pos[i] = 1 ;
    }
    
    sort(pos + 1 , pos + n + 1) ;
    //把这个 01 串从最小开始排列
    
    
    do{
        int sum = 0 ;
        for(int i = 1 ; i <= n ; i++){
            if(pos[i] == 1){
                sum += a[i] ;
            }
        }
        if(is_prime(sum)) ans++ ;
    }while(next_permutation(pos + 1 , pos + n + 1)) ;
    // do while 使这个排列所有次数都用到
    
    
    cout << ans << endl ;
}

signed main(){
    solve() ;
    return 0 ;
}
```







